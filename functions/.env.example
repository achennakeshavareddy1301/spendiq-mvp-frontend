# SpendIQ Cloud Functions Environment Variables
# These are set via Firebase Functions config, not .env file

# To set Gemini API key in Firebase:
# firebase functions:config:set gemini.apikey="YOUR_GEMINI_API_KEY"

# For local emulator testing, create a .runtimeconfig.json file with:
# {
#   "gemini": {
#     "apikey": "YOUR_GEMINI_API_KEY"
#   }
# }

# Alternatively, for Firebase Functions v2, you can use Secret Manager
# or define env variables in firebase.json
